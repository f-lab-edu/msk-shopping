pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/milestone' }
		maven { url 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
	}
}
rootProject.name = 'msk-shopping'

include 'coupon-redis'
include 'auth-user' // new module
include 'global-utils'

